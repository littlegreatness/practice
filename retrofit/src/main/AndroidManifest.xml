<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buxiaoqing.prac">

    <!-- 访问网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Teach1Activity" />
        <activity android:name=".ui.Teach2Activity" />
        <activity android:name=".ui.Teach3Activity" />
        <activity android:name=".ui.Teach4Activity" />
        <activity android:name=".ui.Teach5Activity" />
    </application>

</manifest>